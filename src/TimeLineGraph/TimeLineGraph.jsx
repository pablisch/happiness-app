import TimeLineGraphImage from "./TimeLineGraphImage.jsx";

function TimeLineGraph() {
    return (
        <div style={{ height: "100%", display: "flex", flexDirection: "column", minHeight: 0 }}>
            <TimeLineGraphImage />
        </div>
    );
}

export default TimeLineGraph;
